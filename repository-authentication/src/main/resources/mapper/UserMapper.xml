<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zxy.repository.authentication.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="org.zxy.repository.authentication.entity.User">
        <!--@mbg.generated-->
        <!--@Table sys_user-->
        <id column="user_id" jdbcType="INTEGER" property="userId"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="enabled" jdbcType="INTEGER" property="enabled"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="phone" jdbcType="VARCHAR" property="phone"/>
    </resultMap>
    <sql id="Base_Column_List">
        <!--@mbg.generated-->
        user_id, username, `password`, enabled, create_time, phone
    </sql>
    <!--auto generated by MybatisCodeHelper on 2020-07-03-->
    <select id="selectOneByUsername" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sys_user
        <where>
            <if test="username != null">
                and username = #{username,jdbcType=VARCHAR}
            </if>
        </where>
    </select>

    <select id="selectAuthorityByUserId" resultType="string">
        SELECT code
        FROM sys_menu m
                     LEFT JOIN sys_role_menu rm ON m.menu_id = rm.menu_id
                     LEFT JOIN sys_role r ON r.role_id = rm.role_id
                     LEFT JOIN sys_user_role ur ON ur.role_id = r.role_id
                     LEFT JOIN sys_user u ON u.user_id = ur.user_id
        WHERE u.user_id = #{userId,jdbcType=INTEGER}
    </select>
</mapper>